// Префиксный вариант ++i:

let i = 0;
while (++i < 5) alert(i);

// Постфиксный вариант i++

let j = 0;
while (j++ < 5) alert(j);

// В первом цыкле выведет от 1 дод 4, а вовтором от 1 до 5, т. к. в первом случае сначало увиличивается значение,
// а потм идет сравнение. во втором случае сначало сравнение идет со старым значением
